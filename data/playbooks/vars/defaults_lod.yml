vars_default:
  svm:
    allowed_protocols: cifs
    snapshot_policy: default
    language: c.utf_8
    root_volume_percent_snapshot_space: 0
    root_volume_space_guarantee: none
    root_volume_security_style: ntfs
    
  volume:
    space_guarantee: none
    size_unit: b
    type: dp
    security_style: ntfs
    language: c.utf_8
    snapshot_policy:
      name: none
