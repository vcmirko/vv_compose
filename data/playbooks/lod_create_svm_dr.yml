---
- name: "Bsh Create Migration Svm"
  hosts: localhost
  become: false
  gather_facts: false
  vars_files:
    - "vars/defaults_lod.yml"

  roles:
    - { role: af, qtask: facts }  
    - { role: svm, qtask: facts_dr }
    - { role: svm, qtask: create_dr }    
